# 루넬 시스템 — 개발 로드맵

> 현재 상태에서 실제로 유용하고 혁신적인 시스템으로 만들기 위한 단계별 계획

---

## 📊 현재 상태 진단

| 영역 | 완성도 | 핵심 문제 |
|------|--------|----------|
| **백엔드** | ~85% | 37개 API, 18개 테이블, 8개 서비스 — 대부분 동작 |
| **프론트엔드** | ~30% | 17개 페이지 있지만 대부분 읽기 전용 껍데기 |
| **실사용 가능** | ~15% | 일정 생성/조회만 가능. 편집, 삭제, 프로젝트 생성 불가 |

### 핵심 문제: 백엔드에 혁신이 있는데 프론트엔드가 그걸 전혀 안 쓰고 있음

백엔드에 이미 구현된 혁신적 기능들:
- **다층 중요도 시스템** — 기본값 + 권위 가중치 + 피드백 + 긴급도 + 의존성 = 실시간 점수
- **DAG 순환 방지** — 프로젝트 의존 관계 자동 검증
- **챌린지 인증** — 비밀번호 대신 문제 풀이로 접근 제어
- **시간여행 쿼리** — 과거 임의 시점의 일정/프로젝트 상태 복원
- **기댓값 분석** — 의사결정 지원 도구

→ 이것들이 프론트엔드에서 **하나도** 활용되고 있지 않음.

---

## Phase 1: 기본 CRUD 완성 — "최소한 쓸 수 있게"

> 목표: 모든 엔티티에 대해 생성/조회/수정/삭제가 되어야 함

### 1-1. 일정(Schedule) 편집/삭제
- `schedules/[id]` 상세 페이지에 **편집 모드** 추가 (인라인 또는 모달)
- PATCH `/schedules/{id}` 연동 — 제목, 설명, 날짜, 중요도, 상태 변경
- DELETE `/schedules/{id}` 연동 — 확인 다이얼로그 후 삭제
- 상태 변경 퀵 버튼: 초안→예정→진행중→완료

### 1-2. 프로젝트 생성/편집
- `projects` 목록 페이지에 **새 프로젝트** 버튼 + 생성 폼
- POST `/projects` 연동 — 제목, 설명, 그룹 연결
- `projects/[id]` 상세에 편집/삭제 기능
- PATCH `/projects/{id}` 연동

### 1-3. 그룹 상세 + 멤버 관리
- `groups/[id]` 상세 페이지 신규 생성
- GET `/groups/{id}` — 그룹 정보 + 멤버 목록
- POST `/groups/{id}/members` — 이메일로 멤버 초대
- DELETE `/groups/{id}/members/{user_id}` — 멤버 제거
- 멤버 역할(OWNER/ADMIN/MEMBER/VIEWER) 표시

### 1-4. 일정 상세 강화
- 평가 통계 표시: GET `/ratings/schedules/{id}/ratings-summary` 연동
- 별점 + 유용성 점수 + 코멘트 표시
- 변경 이력 섹션: GET `/history/schedules/{id}/history` 연동
- 관련 프로젝트 링크

---

## Phase 2: 핵심 차별화 기능 — "이건 다른 도구에 없어"

> 목표: 백엔드의 혁신적 기능을 프론트엔드에서 직관적으로 체험할 수 있게

### 2-1. DAG 인터랙티브 시각화
- **React Flow** 라이브러리 도입 (기존 텍스트 DAG 대체)
- 노드 드래그 & 연결: 마우스로 의존 관계 설정
- POST `/activity/projects/{id}/edges` — 간선 추가 (순환 자동 검증)
- DELETE `/activity/projects/{id}/edges/{id}` — 간선 삭제
- 노드 색상: 상태별 (TODO=회색, IN_PROGRESS=파랑, DONE=초록, BLOCKED=빨강)
- 노드 크기: 중요도/진행률 반영
- 레이어 자동 배치: GET `/activity/projects/{id}/tree` 기반

### 2-2. 중요도 점수 시각화 및 분해
- 일정 상세 페이지에 **점수 브레이크다운** 차트 추가
- 막대 그래프로 각 요소 시각화:
  ```
  기본 중요도   ████████████████████ 45/100
  권위 가중치   ██████              12/30
  피드백        ████                 8/20
  긴급도        ██████████████████  18/20
  의존성        ███                  5/10
  ─────────────────────────────────────
  최종 점수                         88
  ```
- 긴급도가 시간 경과에 따라 변하는 **실시간 인디케이터**
- "이 점수는 왜 이렇게 높은가/낮은가" 설명 텍스트 자동 생성

### 2-3. 챌린지 기반 접근 제어 (Procedural Key UI)
- 특정 일정/프로젝트에 접근할 때 **챌린지 모달** 자동 표시
- POST `/challenges` → 문제 수신 (수학 또는 텍스트)
- POST `/challenges/{id}/verify` → 답변 제출
- 남은 시도 횟수, 만료 시간 카운트다운 표시
- 일정/프로젝트 생성 시 **접근 정책 설정** UI
  - POST `/visibility-policies` 연동
  - 범위 선택: 공개 / 인증 / 역할 / 그룹 / 복합 / 챌린지
  - 그룹 허용/차단 목록 설정

### 2-4. 시간여행 히스토리 뷰어
- 히스토리 페이지에 **타임라인 슬라이더** 추가
- GET `/history/schedules/{id}/at?timestamp=...` 연동
- 슬라이더를 움직이면 해당 시점의 일정 상태가 카드로 표시
- 현재 상태와 과거 상태의 **diff 하이라이팅** (변경된 필드 강조)
- "이 시점으로 되돌리기" 기능 (PATCH로 과거 값 복원)

### 2-5. 대회 풀 워크플로우
- **대회 생성**: POST `/competitions` (이벤트 연결, 최대 인원, 채점 규칙)
- **참가 신청**: POST `/competitions/{id}/participants`
- **제출**: POST `/competitions/{id}/submissions` — 텍스트/파일 내용 제출
- **채점** (교사용): PATCH `/competitions/{id}/submissions/{sid}/grade` — 점수 입력
- **리더보드**: POST `/competitions/{id}/scoreboard` (생성) + GET (조회)
- 실시간 순위 테이블 — 순위, 점수, 제출 시간
- 대회 상태 플로우: 접수중 → 진행중 → 심사중 → 완료

---

## Phase 3: 스마트 기능 — "알아서 해줘"

> 목표: 사용자가 명시적으로 요청하지 않아도 시스템이 인사이트를 제공

### 3-1. 스마트 대시보드
- **충돌 감지**: 같은 시간대에 겹치는 일정 자동 경고
- **워크로드 히트맵**: 요일별/주차별 일정 밀도 시각화
- **이번 주 요약**: "이번 주에 마감 3건, 프로젝트 진행률 67%"
- **긴급 알림 배너**: D-3 이내 마감 일정 상단 고정
- **그룹별 활동 요약**: 최근 활동이 많은/적은 그룹 하이라이트

### 3-2. 전역 검색
- **Cmd/Ctrl + K** 커맨드 팔레트 스타일 검색
- 일정, 프로젝트, 그룹, 대회 통합 검색
- 최근 방문, 즐겨찾기 빠른 접근
- 액션 검색: "새 일정 만들기", "그룹 만들기" 등

### 3-3. 자동 마감 알림
- 백엔드에 주기적 스케줄러 추가 (APScheduler 또는 Celery Beat)
- 24시간 전, 6시간 전, 1시간 전 자동 알림 생성
- `notify_deadline_approaching()` 서비스 함수 자동 호출
- 임시 그룹 만료 자동 비활성화: `deactivate_expired_groups()`

### 3-4. iCal 내보내기
- 백엔드에 GET `/schedules/export/ical` 엔드포인트 추가
- ICS 파일 다운로드 → Google Calendar, Apple Calendar 연동
- 캘린더 구독 URL 생성 (자동 동기화)

### 3-5. 일정 템플릿
- 자주 만드는 일정 유형을 템플릿으로 저장
- "주간 회의", "과제 제출", "시험 일정" 등 프리셋
- 템플릿 선택 → 필드 자동 채움 → 날짜만 변경

---

## Phase 4: 완성도 — "프로답게"

> 목표: 매일 쓸 수 있을 만큼 만족스러운 UX

### 4-1. 모바일 반응형 최적화
- 사이드바 → 하단 탭 바 (모바일)
- 터치 친화적 버튼 크기 (최소 44px)
- 스와이프 제스처 (캘린더 월 이동, 알림 읽음 처리)
- PWA manifest + 서비스 워커

### 4-2. 키보드 단축키
- `N` — 새 일정 만들기
- `←/→` — 캘린더 월 이동
- `Esc` — 모달/폼 닫기
- `?` — 단축키 안내 표시

### 4-3. 드래그 앤 드롭 캘린더
- 캘린더에서 일정 드래그로 날짜 변경 → PATCH 자동 호출
- 드래그로 새 일정 생성 (시간 범위 선택)

### 4-4. 데이터 내보내기
- CSV 내보내기: 일정 목록, 프로젝트 목록, 대회 결과
- PDF 리포트: 프로젝트 진행 보고서, 대회 결과 보고서
- 차트 이미지 내보내기

### 4-5. 실시간 업데이트
- WebSocket 또는 SSE(Server-Sent Events)
- 알림 실시간 수신 (폴링 대체)
- 대회 리더보드 실시간 업데이트
- 동시 편집 충돌 방지

---

## 실행 우선순위

```
Phase 1 (Foundation)        ██████████████████████████ 필수 — 이것 없으면 아무것도 못 함
Phase 2 (Differentiators)   ████████████████████      핵심 — 루넬만의 가치
Phase 3 (Intelligence)      ████████████████          유용 — 일상적 활용도
Phase 4 (Polish)            ████████████              완성 — 프로 수준
```

---

## 기술 의사결정

| 결정 사항 | 선택 | 이유 |
|-----------|------|------|
| DAG 시각화 | **React Flow** | 가장 성숙한 React 노드 에디터, 커스터마이징 용이 |
| 차트 | **Recharts** | shadcn/ui와 스타일 일관성, 가벼움 |
| 커맨드 팔레트 | **cmdk** | shadcn/ui에 이미 Command 컴포넌트 존재 |
| 스케줄러 | **APScheduler** | Celery보다 가벼움, FastAPI 내장 가능 |
| 드래그 앤 드롭 | **@dnd-kit** | React 18 호환, 접근성 지원 |
| iCal | **icalendar** (Python) | 표준 RFC 5545 구현 |
